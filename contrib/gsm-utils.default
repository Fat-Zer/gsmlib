# /etc/default/gsm-utils: These settings overwrite the defaults that are
#                         used in the gsmsms*-scripts

PHONEDEV=/dev/mobilephone  # or /dev/ttyS0 or /dev/ircomm0 or /dev/ttyUSB0
BAUDRATE=9600
PIN=""  # or 1234

SPOOLDIR=/var/spool/sms
PRIORITIES=3

SMSADMIN=root
SUBJECT="SMS delivery report:"

SMSPROCESSOR=""  # or /usr/bin/gsmsmsprocessor

function do_accounting {
 if [ "$2" = "sent" ]; then
  true # it's your turn
 fi
}
